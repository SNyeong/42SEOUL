#include "libft.h"
#include <stdio.h>

int dp[1001];

int main(void)
{
	int i;
	i = ft_atoi("  -123");
	printf("%d\n", i);
	

/*	while (i < 4)
		printf("c = %d\n", c[i++]);
	i = 0;
	d = b;
	while (i < (int)sizeof(b))
		printf("d1 = %d\n", d[i++]);
	i = 0;
	d = memcpy(a, b, 3);
	while (i < (int)sizeof(b))
		printf("d = %d\n", d[i++]);
	printf("%d\n", i);*/
}